# Frontend (Vite + React)

Instruksi singkat:

1. Install dependencies

```bash
cd frontend
npm install
```

2. Jalankan dev server

```bash
npm run dev
```

Build untuk produksi:

```bash
npm run build
```
